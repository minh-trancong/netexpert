/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['via.placeholder.com', 'blog.nperf.com', 'images.pexels.com', 'image.nhandan.vn'],
  },
}

module.exports = nextConfig
